// Canonical area widget configuration. Import this and use it for area `widgets` options
// rather than building a custom config from scratch — keeps allowed widgets consistent
// across page types and reduces schema drift.
export default {
  '@apostrophecms/image': {},
  '@apostrophecms/video': {},
  '@apostrophecms/rich-text': {}
};
